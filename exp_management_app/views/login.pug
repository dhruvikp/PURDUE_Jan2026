extends layout
block content
    h2 Login

    form(action="/login" method="POST")
      input(type="text" name="username" placeholder="Username" data-cy="username")
      input(type="password" name="password" placeholder="Password" data-cy="password")
      button(type="submit" data-cy="login") Login